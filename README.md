1. Sync with build gradle first to import all necessary classes and files
2. Only one main activity to stage fragments, all logics should be written in fragments
3. Utilize local storage
4. External API gemini is connected, code is done(parse in own individual files, raw response string from AI -> JSON array -> JSON object 
